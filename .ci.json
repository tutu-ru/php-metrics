{
  "env": {
    "resources": [
    ]
  },
  "unittest": {
    "command": "yum install -y php-xdebug && php vendor/bin/phpunit -c phpunit.xml --log-junit /reports/phpunit.xml --coverage-clover /reports/clover.xml"
  }
}
